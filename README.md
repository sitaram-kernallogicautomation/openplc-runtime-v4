# openplc-runtime
OpenPLC Runtime v4 designed to run programs built on OpenPLC Editor v4

## 🛠️ Build Instructions

1. **Copy Generated Files**
   Copy the files generated by the OpenPLC IDE into the `/core/generated` directory.

2. **Build the Project**
   Run the following commands from the project root:

   ```bash
   mkdir build
   cd build
   cmake ..
   make

3. **Compile application generated**
   Run the following commands from the project root:

   ```bash
   ./scripts/compile.sh
